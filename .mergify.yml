pull_request_rules:
  - name: p30 simple comment
    conditions:
      - base=main
    actions:
      comment:
        message: "p30-alive author={{author}} n={{number}} title={{title}}"
      label:
        add:
          - p30-probe
