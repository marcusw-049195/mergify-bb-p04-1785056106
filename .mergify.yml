queue_rules:
  - name: default
    batch_size: 1
    queue_conditions:
      - base=main
    merge_conditions: []
    merge_method: merge

pull_request_rules:
  - name: p33 auto queue
    conditions:
      - base=main
      - label=queued
    actions:
      queue:
        name: default
